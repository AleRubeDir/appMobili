/*
package it.uniupo.progetto.fragments

import android.os.Bundle
import android.util.Log
import android.widget.NumberPicker
import android.widget.TextView
import androidx.appcompat.app.AppCompatActivity
import it.uniupo.progetto.Prodotto
import it.uniupo.progetto.R

class ItemPage (p : Prodotto): AppCompatActivity() {
*/
/*    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.pagina_prodotto)
        val titolo = intent.getStringExtra("titolo_prodotto")
        // val values : Array<Prodotto> = intent.getSerializableExtra("values") as ArrayList<Prodotto>
        val p : Prodotto? = Prodotto.getProdottoFromDB(titolo!!)
        Log.d("PagProd","$p")
        val tvTitolo = findViewById<TextView>(R.id.titolo)
        tvTitolo.text = p!!.titolo
        val desc = findViewById<TextView>(R.id.desc)
        desc.text = p!!.desc
        val prezzo = findViewById<TextView>(R.id.prezzo)
        prezzo.text = p!!.prezzo.toString()
        val np = findViewById<NumberPicker>(R.id.qta)
        np.minValue = 1
        np.maxValue = p!!.qta
    }*//*

}*/
